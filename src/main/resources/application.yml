server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_foot
    username: root
    password: 
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
  application:
    name: WINNER-SERVICE